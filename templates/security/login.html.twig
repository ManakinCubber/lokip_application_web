{% extends 'base.html.twig' %}

{% block title %}Se connecter{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/login.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header text-center">
                        <img src="{{ asset('images/logo.png') }}" alt="Logo Google" class="img-fluid">
                        <h2>Connexion</h2>
                    </div>
                    <div class="card-body">
                        {% if error %}
                            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}
                        <p>Accédez au panel en toute sécurité en utilisant votre compte Google. Cliquez simplement sur le bouton ci-dessous pour continuer.</p>
                        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google'}) }}" class="btn btn-primary btn-block">
                            <img src="{{ asset('images/logo_google.png') }}" alt="Logo Google" class="img-fluid" style="width: 20px;">
                            Se connecter avec Google
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}